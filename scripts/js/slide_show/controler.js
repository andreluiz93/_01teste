var controler = {
    
        find: function(idElement) {
        var id = document.getElementById(idElement);
        return id;
    },
    
    findValue: function (idElement){
    var value;
    }
    
}